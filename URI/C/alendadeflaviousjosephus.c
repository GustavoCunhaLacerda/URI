#include <stdio.h>
#include <stdlib.h>
typedef unsigned int uint;
typedef unsigned long long int ulli;

int main(void){
    int nc;
    scanf("%d", &nc);
    for (int i = 0; i < nc; i++)
    {
        int salto, aux;
        scanf("%d %d", &aux, &salto);
        int pessoas[aux], vivas = aux;
        for (int j = 0; j < pessoas; i++)
        {
            
        }
        
    }
    
    return 0;
}